apiVersion: v1
kind: Service
metadata:
  name: {{ include "pidp-support.fullname" . }}
  labels:
    {{- include "pidp-support.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pidp-support.selectorLabels" . | nindent 4 }}
