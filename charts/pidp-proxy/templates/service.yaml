apiVersion: v1
kind: Service
metadata:
  name: {{ include "pidp-proxy.fullname" . }}
  labels:
    {{- include "pidp-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pidp-proxy.selectorLabels" . | nindent 4 }}
